# Project Selection

Section heading on all V3 CVs: **SELECTED PROJECTS**

Default counts: 2 (one-page), 2–3 (two-page).

Weights: see `PROJECT_RANK_WEIGHTS` / `docs/RESUME_ENGINE_V3.md`.

AI Engineer / Applied AI paths force-include Aethelgard + CareerOS when both are `approvedForCV`.

RedVelvetVault remains eligible but is not forced onto AI Engineer CVs.
